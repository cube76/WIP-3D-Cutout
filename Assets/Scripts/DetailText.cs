using UnityEngine;

public class DetailText : MonoBehaviour
{
    private ChangeText changeTextScrpt;
    // Start is called before the first frame update
    void Start()
    {
        changeTextScrpt = GetComponent<ChangeText>();
    }

    // Update is called once per frame
    void Update()
    {
    }
}
